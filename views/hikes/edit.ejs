<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= hike.trailName %>
    </title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
        <h1>Edit <%= hike.trailName %>
        </h1>
        <div class="form-container">
            <form action="/hikes/<%= hike._id %>?_method=PUT" method="POST">
                <label for="trailName">Trail Name:</label>
                <input type="text" name="trailName" id="trailName" value="<%= hike.trailName %>" required />
                <label for="dateHiked">Date:</label>
                <input type="date" name="dateHiked" id="dateHiked" value="<%= hike.dateHiked ? new Date(hike.dateHiked).toISOString().split('T')[0] : '' %>" />
                <label for="location">Location:</label>
                <input type="text" name="location" id="location" value="<%= hike.location %>" />
                <label for="distance">Distance (mi):</label>
                <input type="number" name="distance" id="distance" value="<%= hike.distance %>" />
                <label for="elevationGain">Elevation Gain (ft):</label>
                <input type="number" name="elevationGain" id="elevationGain" value="<%= hike.elevationGain %>" />
                <label for="difficulty">Difficulty:</label>
                <select name="difficulty" id="difficulty">
                    <option value="easy" <%=hike.difficulty==='easy' ? 'selected' : '' %>>Easy</option>
                    <option value="moderate" <%=hike.difficulty==='moderate' ? 'selected' : '' %>>Moderate</option>
                    <option value="hard" <%=hike.difficulty==='hard' ? 'selected' : '' %>>Hard</option>
                </select>
                <label for="routeType">Route Type:</label>
                <select name="routeType" id="routeType">
                    <option value="loop" <%=hike.routeType==='loop' ? 'selected' : '' %>>Loop</option>
                    <option value="out and back" <%=hike.routeType==='out and back' ? 'selected' : '' %>>Out and Back
                    </option>
                </select>
                <label for="favorite">Add to Favorites?</label>
                <input type="checkbox" name="favorite" id="favorite" value="true" <%= hike.favorite ? 'checked' : '' %>>
                <button type="submit">Update Hike</button>
            </form>
        </div>
</body>

</html>